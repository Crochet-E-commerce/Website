<%- include('../partials/head') %>

    <body class="p-12">
        <header>
            <%- include('../partials/nav') %>
        </header>
        <main>

            <div class="flex flex-col gap-6 my-10">
                <h3 class="text-center text-xl font-bold">Categories</h3>
                <h1 class="text-center">
                    <%= tagline %>
                </h1>
                <div class="flex justify-between">
                    <% categories.forEach(function(category,index) { %>

                        <a href="/shop/<%= index %>"
                            class="flex flex-col justify-center items-center w-48 h-48 rounded-2xl shadow-md p-5 mb-4">
                            <strong>
                                <%= category.name %>
                            </strong>
                            <img src="/category/<%= category.img %>.png" alt="<%= category.name %>"
                                class="w-10 h-10 sm:w-16 sm:h-16 md:w-20 md:h-20 lg:w-20 lg:h-20 xl:w-32 xl:h-32" />
                        </a>

                        <% }); %>
                </div>
            </div>

            <div class="flex gap-16">
                <div class="w-2/3">
                    <div class="flex gap-2 rounded-2xl p-5 bg-pink-100">
                        <p>✨</p>
                        <h3 class="font-bold">Special Offers</h3>
                    </div>
                </div>

                <div class="w-full">
                    <div>
                        <div class="flex gap-2 p-5 rounded-2xl bg-pink-200  mb-5">
                            <p>👍🏼</p>
                            <h3 class="font-bold">Trending Products</h3>
                        </div>
                        <%- include('../partials/lists/trendingLists') %>
                    </div>
                    <div>
                        <div class="flex gap-2 p-5 rounded-2xl bg-pink-200 mb-5 mt-8">
                            <p>🎀</p>
                            <h3 class="font-bold">Popular Picks</h3>
                        </div>
                        <%- include('../partials/lists/popularLists') %>
                    </div>
                </div>
            </div>
        </main>
    </body>
    <%- include('../partials/footer') %>